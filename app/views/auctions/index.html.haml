.row
  .col-md-3
    = render 'common/categories/categories'
  .col-md-9
    %h1
      = "「#{@category.name}」のオークション 一覧"
    %hr
    - if @auctions.blank?
      %p.text-muted
        このカテゴリで、現在開催されているオークションは存在しません。
    - else
      - @auctions.each do |auction|
        .auction
          = link_to [auction.category, auction] do
            %ul.list-inline
              %li
                %img.img-thumbnail{src: 'http://placeimg.com/150/120/tech'}
              %li
                %dl.dl-horizontal
                  %dt タイトル :
                  %dd= auction.title
                  %dt 現在の最高入札金額 :
                  %dd= "#{auction.highest_price} 円"
                  %dt 終了日時 :
                  %dd= auction.close_at
