- @auctions.each do |auction|
  .auction
    = link_to [auction.category, auction] do
      %ul.list-inline
        %li
          - if auction.picture?
            = image_tag auction.picture.url, size: '150x120'
          - else
            = image_tag 'noimage.png', size: '150x120'
        %li
          %dl.dl-horizontal
            %dt タイトル :
            %dd= auction.title
            %dt 現在の最高入札金額 :
            %dd= "#{number_with_delimiter(auction.highest_price)} 円"
            %dt 終了日時 :
            %dd= auction.close_at