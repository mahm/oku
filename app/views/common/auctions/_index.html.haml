- @auctions.each do |auction|
  .auction
    = link_to [auction.category, auction] do
      %ul.list-inline
        %li
          -#%img.img-thumbnail{src: 'http://placeimg.com/150/120/tech'}
          - if auction.picture?
            = image_tag auction.picture.url, size: '150x120'
          - else
            = image_tag 'noimage.png', size: '150x120'
        %li
          %dl.dl-horizontal
            %dt タイトル :
            %dd= auction.title
            %dt 現在の最高入札金額 :
            %dd= "#{auction.highest_price} 円"
            %dt 終了日時 :
            %dd= auction.close_at