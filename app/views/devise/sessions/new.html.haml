.row
  .col-md-4.col-md-offset-4
    %h1 サインイン
    %hr
    .alert.alert-info
      ユーザー登録がまだの方は
      = link_to 'こちらへ', new_registration_path(resource_name), class: 'label label-primary'
    = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .form-inputs
        = f.input :email, autofocus: true
        = f.input :password
      .form-actions
        = f.button :submit, 'サインイン'
