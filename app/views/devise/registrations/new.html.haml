.row
  .col-md-4.col-md-offset-4
    %h1 新規ユーザー登録
    .alert.alert-info
      ユーザー登録済みの方は
      = link_to 'こちらへ', new_session_path(resource_name), class: 'label label-primary'
    = simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      .form-inputs
        = f.input :email, required: true, autofocus: true
        = f.input :password, required: true, hint: ("#{@minimum_password_length} 文字以上で指定して下さい" if @validatable)
        = f.input :password_confirmation, required: true
      .form-actions
        = f.button :submit, '新規ユーザー登録'
